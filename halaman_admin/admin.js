// JavaScript for admin dashboard

// Fungsi untuk logout
document.querySelector('.top-bar button').addEventListener('click', function() {
  alert("You have logged out!");
  // Kode untuk proses logout bisa ditambahkan di sini
});
